import random

def guess_number():
   
    number = random.randint(1, 100)
    
    attempts = 0
    print("Я загадал число от 1 до 100.")
    
    while True:
        try:
            user_guess = int(input("Введи своё предположение: "))
            attempts += 1
            
            if user_guess < number:

